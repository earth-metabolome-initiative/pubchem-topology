# Production Zenodo API token used by the main binary.
ZENODO_TOKEN=

# Optional sandbox token used by the integration test that exercises the full
# compute -> Parquet -> publish flow against sandbox.zenodo.org.
ZENODO_SANDBOX_TOKEN=
